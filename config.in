# Uncomment and change configuration for enabling an option

# WITH_STM32 = 1		# Generate code for STM32 board
# WITH_PYTHON = 1		# Build Python module.
# WITH_ATMEGA328P = 1	# Generate code for ATMEGA328P board

# Generate code for ARM v5t core
# ARMV5T_PATH=armv5t

# Use Python's UI Orchid at provided path
# ORCHID_PATH=Orchid
